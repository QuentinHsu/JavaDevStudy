/*
    数学函数与常量
 */
public class MathFunction {
    public static void main(String[] args) {
        int x = 2;
        int y = 4;
        System.out.println("2 的 3 次方是：" + Math.pow(2, 3));
        System.out.println("2 的 4 次方是：" + Math.pow(x, y));
    }
}
